function [cl,cd,cm] = aero_coeff(x,y,cp,al,npanel)


cl = 0;
cd = 0;

for i=1:npanel
    dx  = x(i+1) -x(i);
    dy  = y(i+1) -y(i);
    dcl = -cp(i)*dx;
    dcd = cp(i)*dy;
    cl  = cl +dcl;
    cd  = cd +dcd;
end

dcl = cl*cos(al) -cd*sin(al);
cd  = cl*sin(al) +cd*cos(al);
cl  = dcl;


return
